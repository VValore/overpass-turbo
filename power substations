/*
This has been generated by the overpass-turbo wizard.
The original search was:
“power=substation in "Copenhagen Municipality"”
*/
[out:json][timeout:25];
// fetch area “Copenhagen Municipality” to search in
(
  //{{geocodeArea:Copenhagen Municipality}};
  //{{geocodeArea:Gentofte Municipality}};
  //{{geocodeArea:Lyngby-Taarbæk Municipality}};
  //{{geocodeArea:Rudersdal Municipality}};
  //{{geocodeArea:Frederiksberg Municipality}};
  //{{geocodeArea:Rødovre Municipality}};
  //{{geocodeArea:Hvidovre Municipality}};
  //{{geocodeArea:Glostrup Municipality}};
  //{{geocodeArea:Albertslund Municipality}};
  {{geocodeArea:Viborg Municipality}};
  //{{geocodeArea:Roskilde Municipality}};
 //{{geocodeArea:Zealand}};
)->.searchArea;
// gather results
(
  // query part for: “power=substation”
  node["power"="substation"](area.searchArea);
  way["power"="substation"](area.searchArea);
  relation["power"="substation"](area.searchArea);
)->.substations;
(
  // query part for: “power=substation”
  node["power"](area.searchArea);
  way["power"](area.searchArea);
  relation["power"](area.searchArea);
)->.all_power;
(
  node["street_cabinet"="power"](area.searchArea);
  way["street_cabinet"="power"](area.searchArea);
  relation["street_cabinet"="power"](area.searchArea);
)->.street_cabinets;
(
  node["power"="tower"](area.searchArea);
  way["power"="tower"](area.searchArea);
  relation["power"="tower"](area.searchArea);
)->.towers;
( .all_power; - .substations;)->.all_power;
( .all_power; - .towers; )->.all_power;
( .substations; .street_cabinets; .all_power; );
// print results
out meta;
>;
out skel qt;

{{style:

/* A catch-all for nodes that is not specifically handled */
node, are
{ color:black; fill-color:white; }

/* street_cabinet will have a blue outline */
node[man_made=street_cabinet]
{ color:blue; fill-color:green; }

/* substations (both nodes and ways) will have a green outline */
node[power=substation],
way[power=substation]
{ color:green; fill-color:red; }



node[power=tower]
{ color:white; fill-color:gray; }

way[power=line]
{ color:black; width:2; opacity:4; }

way[power=cable]
{ color:black; width:2; dashes:3,3; opacity:4; }

}}
